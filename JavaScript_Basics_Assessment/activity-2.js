const name = prompt('Enter your first name');
        if (name.length == 4) {
        alert('Your name is 4 characters')
    }
        if (name.length > 4) {
        alert('Your name is longer than 4 characters')
    }
        if (name.length <4 ) {
        alert('your name is shorter than 4 characters');
    }
